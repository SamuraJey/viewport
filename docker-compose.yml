services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-backend
    container_name: viewport_app
    env_file:
      - .env.prod
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      - s3-service
    networks:
      - backend
    environment:
      - S3_ENDPOINT=http://s3-service:9000
      - S3_USE_SSL=false
      - S3_ACCESS_KEY=rustfsadmin
      - S3_SECRET_KEY=rustfsadmin
      - S3_BUCKET=viewport
    volumes:
      - ./src:/app/src
      - ./config:/app/config
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 1G
        reservations:
          cpus: "0.25"
          memory: 256M

  postgres:
    image: postgres:17-alpine
    container_name: viewport_postgres
    restart: always
    environment:
      POSTGRES_DB: viewport
      POSTGRES_USER: viewport
      POSTGRES_PASSWORD: viewport
      # Increase max connections for concurrent Celery tasks
      POSTGRES_MAX_CONNECTIONS: 100
    command: postgres -c max_connections=100 -c shared_buffers=256MB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.25"
          memory: 128M

  s3-service:
    image: rustfs/rustfs:1.0.0-alpha.80
    container_name: viewport_s3
    restart: always
    environment:
      # RUSTFS_ADDRESS: ':9000'
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: true
      RUSTFS_CORS_ALLOWED_ORIGINS: "*"
      RUSTFS_NOTIFY_WEBHOOK_AUTH_TOKEN: some_secure_token
      RUSTFS_NOTIFY_WEBHOOK_ENABLE: true
      RUSTFS_BUFFER_PROFILE: AiTraining
      RUSTFS_NS_SCANNER_INTERVAL: 60  # in seconds
      RUST_LOG: "rustfs=info,ecstore=info,s3s=info,iam=info,notify=info"

    # command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - s3_data:/data
    networks:
      - backend
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 1G
        reservations:
          cpus: "0.25"
          memory: 128M

  redis:
    image: valkey/valkey:8-alpine
    container_name: viewport_redis
    restart: always
    command: >
      valkey-server
      --maxclients 10000
      --tcp-backlog 511
      --timeout 0
      --tcp-keepalive 300
    ports:
      - "6379:6379"
    networks:
      - backend
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 256M
        reservations:
          cpus: "0.10"
          memory: 32M

  celery_worker:
    build:
      context: .
      dockerfile: Dockerfile-backend
    container_name: viewport_celery_worker
    command: celery -A src.viewport.celery_app worker --loglevel=info --concurrency=4 --max-tasks-per-child=500 --beat
    env_file:
      - .env.prod
    depends_on:
      - redis
      - postgres
      - s3-service
    networks:
      - backend
    environment:
      - S3_ENDPOINT=http://s3-service:9000
      - S3_USE_SSL=false
      - S3_ACCESS_KEY=rustfsadmin
      - S3_SECRET_KEY=rustfsadmin
      - S3_BUCKET=viewport
    volumes:
      - ./src:/app/src
      - ./config:/app/config
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 1700M
        reservations:
          cpus: "0.50"
          memory: 256M

volumes:
  postgres_data:
  s3_data:

networks:
  backend:
    driver: bridge
